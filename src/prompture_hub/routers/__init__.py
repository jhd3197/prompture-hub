"""HTTP routers grouped by surface.

- ``openai_compat`` — OpenAI-compatible endpoints (drop-in for OpenAI SDKs).
- ``extract`` — Prompture-native structured extraction.
- ``admin`` — Key CRUD + usage browsing (gated by HUB_ADMIN_TOKEN).
- ``dashboard`` — Server-rendered HTML UI.
"""
